

#include "Common.h"
#include <vector>

/// <summary>
/// 寻找最后一个符合Op的元素
/// </summary>
/// <returns> 最后一个元素的位置 </returns>

template<typename ElemType, typename Operation>
std::vector<ElemType>::iterator FindLastElemPos(std::vector<ElemType> Container, std::vector<ElemType>::iterator Start,
	std::vector<ElemType>::iterator End, Operation Op)
{
	
}

template<typename ElemType, typename Operation>
int FindLastElemPos(std::vector<ElemType> Container, Operation Op)
{

}

template<typename ElemType, typename Operation>
int FindLastElemPos(std::vector<ElemType> Container, Operation Op)
{

}

template<typename ElemType, typename Operation>
int FindLastElemPos(std::vector<ElemType> Container, Operation Op)
{

}